设计一套个人知识网站，中文界面，桌面端 1280px，同时给出手机/平板端 767px。项目由 3 个相互关联的模块组成：Chat、Notes、Works。根路径直接进入 Chat，不设计独立首页。

定位：独立开发者的个人知识台，不是公司官网，也不是 SaaS 后台。语气克制、好读、偏编辑感。

构图原则：视觉系统锁定，页面版式不锁死。三个模块共用同一套色彩、字体、间距和导航，但每页的区块节奏、对齐方式与主次关系可以根据内容自然变化。宁可减少区块、增加留白，也不要为了显得完整而堆叠卡片。

当前固定使用浅色主题，保留主题切换能力但不展示切换入口：

- 页面背景使用由淡蓝 `rgba(56, 189, 248, 0.2)`、淡紫 `rgba(192, 132, 252, 0.1)` 到白色的纵向渐变
- elevated 使用半透明白色，并配合轻度背景模糊
- border 使用 `rgba(255, 255, 255, 0.6)`
- text 使用接近黑色，muted 使用 #555
- accent 使用 #FF5E3A，仅用于当前导航和少量重点状态

代码块使用浅灰底，语法高亮不超过 3 种低饱和色。不要霓虹光晕或高强度阴影。文章区域最大宽度 720px，正文行高 1.7。

设计系统（三页共用）：

- 标题使用有个性的衬线或几何无衬线，正文使用干净无衬线
- 使用 8px 间距网格，区块留白充足
- 圆角控制在 8–12px，以细边框和轻分割线为主，少用阴影
- 强调色只使用一种，其余保持黑白灰
- 顶栏采用悬浮胶囊样式：左侧 Logo，中间导航「Chat / Notes / Works」，右侧用户头像；主题切换暂时隐藏
- 当前导航项明确高亮；Logo 点击后进入 Chat
- 底栏保持极简：一句简介和社交链接占位

页面 1 · Chat

- 作为网站默认入口和核心工具
- 全屏对话布局，可包含可折叠的会话列表与主对话区
- 空状态说明这是「询问我的笔记和作品」的个人助手
- 提供 3 个快捷问题：「解释这篇笔记」「帮我回顾这个作品」「今天学什么」
- 输入框固定在底部，消息区分用户与助手
- 未登录时输入框不可用，发送位置显示登录入口
- 不要客服工单、付费升级条或企业后台式信息密度

页面 2 · Notes

- 顶部或侧边提供分类（全部 / 前端 / 设计 / 读书）和搜索
- 主列表包含标题、两行摘要、日期与标签，按时间倒序
- 可提供热门标签和最近更新，但不要做成复杂后台侧栏
- 整体像有编辑感的个人笔记列表，不像文档管理系统

页面 3 · Works

- 提供筛选：全部 / 网站 / 小工具 / 实验
- 使用 2 列或 3 列作品网格，展示封面、项目名、一行描述和技术标签
- 悬停时显示「查看」
- 不要瀑布流，也不要像 Dribbble 一样过密

响应式要求：

- 桌面端保持清晰的横向导航和充足留白
- 手机端重新组织阅读顺序，不要机械地把桌面栏位压成一列
- 导航、筛选、会话列表和作品网格应有明确的移动端状态

输出：三个模块使用同一套视觉语言，图层结构和组件命名清晰，便于导入 Figma 后继续修改文案。
