# 仅在 Next.js 服务端使用，不会暴露给浏览器。
# `pnpm dev` 会读取 .env.development。
# chat：对话与会话，本地默认 http://localhost:9998。未设置时仍兼容旧变量 API_URL。
CHAT_API_URL=http://localhost:9998
# site：登录与账号，本地默认 http://localhost:9997。
SITE_API_URL=http://localhost:9997

# 可进入 /admin 的用户名，逗号分隔，需与登录名完全一致。留空则所有账号都无法进入后台。
ADMIN_USERS=
