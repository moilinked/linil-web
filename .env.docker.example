# docker compose 变量。复制为 .env 后按环境修改。

IMAGE_TAG=latest
PORT=3030

# 加入后端 compose 默认网络后，用服务名 chat-agent 调用容器内 9998。
# 后端只把 9998 绑在 127.0.0.1，host.docker.internal 进不去。
API_URL=http://chat-agent:9998
CHAT_AGENT_NETWORK=chat-agent_default
